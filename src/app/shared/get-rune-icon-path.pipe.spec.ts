import { GetRuneIconPathPipe } from './get-rune-icon-path.pipe';

describe('GetRuneIconPathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRuneIconPathPipe();
    expect(pipe).toBeTruthy();
  });
});

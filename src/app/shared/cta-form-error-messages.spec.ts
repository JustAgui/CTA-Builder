import { CtaFormErrorMessages } from './cta-form-error-messages';

describe('CtaFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new CtaFormErrorMessages()).toBeTruthy();
  });
});

import { GetStatIconPathPipe } from './get-stat-icon-path.pipe';

describe('GetStatIconPathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetStatIconPathPipe();
    expect(pipe).toBeTruthy();
  });
});
